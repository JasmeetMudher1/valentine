/** @type {import('next').NextConfig} */
const nextConfig = {
    // If you're deploying to a subdirectory like "your-repo-name"
    basePath: '/valentine',  // Replace with your actual repo name
};

export default nextConfig;
